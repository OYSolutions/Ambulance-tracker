package mine.ambulance_service.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SwaggerConfig {
    // Swagger configuration temporarily removed to fix build issues
}